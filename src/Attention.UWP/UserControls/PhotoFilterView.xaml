<UserControl
    x:Class="Attention.UWP.UserControls.PhotoFilterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:ic="using:Microsoft.Xaml.Interactions.Core"
    xmlns:local="using:Attention.UWP.UserControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tc="using:Attention.UWP"
    d:DesignHeight="720"
    d:DesignWidth="320"
    mc:Ignorable="d">

    <Grid>
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition IsStaggeringEnabled="True" />
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="72" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Button
                Margin="12,0"
                VerticalAlignment="Bottom"
                Command="{x:Bind ViewModel.BackCommand}">
                <Button.Content>
                    <FontIcon
                        FontFamily="Segoe MDL2 Assets"
                        FontSize="16"
                        Glyph="&#xE72B;" />
                </Button.Content>
            </Button>
            <!--<ToggleSwitch
                HorizontalAlignment="Center"
                IsOn="{Binding IsOn, Mode=TwoWay}"
                Style="{StaticResource ThemeToggleSwitchStyle}">
                <ToggleSwitch.OffContent>
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE706;" />
                </ToggleSwitch.OffContent>
                <ToggleSwitch.OnContent>
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE708;" />
                </ToggleSwitch.OnContent>
            </ToggleSwitch>-->
        </Grid>
        <Grid Grid.Row="1" Margin="12">
            <AutoSuggestBox
                x:Uid="photo_Find"
                HorizontalContentAlignment="Stretch"
                VerticalContentAlignment="Center"
                Header="Find"
                QueryIcon="Find"
                Text="{Binding Filter.Query, Mode=TwoWay}">
                <i:Interaction.Behaviors>
                    <ic:EventTriggerBehavior EventName="QuerySubmitted">
                        <ic:InvokeCommandAction Command="{x:Bind ViewModel.SearchCommand}" />
                    </ic:EventTriggerBehavior>
                </i:Interaction.Behaviors>
            </AutoSuggestBox>
        </Grid>
        <NavigationViewItemSeparator Grid.Row="2" />
        <StackPanel Grid.Row="3" Margin="12,0">
            <ComboBox
                Grid.Row="3"
                HorizontalAlignment="Stretch"
                ItemsSource="{x:Bind ViewModel.Orders, Mode=OneWay}"
                SelectedItem="{Binding Filter.Order, Mode=TwoWay}"
                SelectionChangedTrigger="Committed">
                <ComboBox.Header>
                    <NavigationViewItemHeader x:Uid="photo_Orders" Content="Orders" />
                </ComboBox.Header>
                <i:Interaction.Behaviors>
                    <ic:EventTriggerBehavior EventName="SelectionChanged">
                        <ic:InvokeCommandAction Command="{x:Bind ViewModel.SelectionChangedCommand}" />
                    </ic:EventTriggerBehavior>
                </i:Interaction.Behaviors>
            </ComboBox>
            <ComboBox
                HorizontalAlignment="Stretch"
                ItemsSource="{x:Bind ViewModel.Orientations, Mode=OneWay}"
                SelectedItem="{Binding Filter.Orientation, Mode=TwoWay}"
                SelectionChangedTrigger="Committed">
                <ComboBox.Header>
                    <NavigationViewItemHeader x:Uid="photo_Orientation" Content="Orientation" />
                </ComboBox.Header>
                <i:Interaction.Behaviors>
                    <ic:EventTriggerBehavior EventName="SelectionChanged">
                        <ic:InvokeCommandAction Command="{x:Bind ViewModel.SelectionChangedCommand}" />
                    </ic:EventTriggerBehavior>
                </i:Interaction.Behaviors>
            </ComboBox>
            <ComboBox
                HorizontalAlignment="Stretch"
                ItemsSource="{x:Bind ViewModel.ImageTypes, Mode=OneWay}"
                SelectedItem="{Binding Filter.ImageType, Mode=TwoWay}"
                SelectionChangedTrigger="Committed">
                <ComboBox.Header>
                    <NavigationViewItemHeader x:Uid="photo_ImageType" Content="Type" />
                </ComboBox.Header>
                <i:Interaction.Behaviors>
                    <ic:EventTriggerBehavior EventName="SelectionChanged">
                        <ic:InvokeCommandAction Command="{x:Bind ViewModel.SelectionChangedCommand}" />
                    </ic:EventTriggerBehavior>
                </i:Interaction.Behaviors>
            </ComboBox>
            <ComboBox
                HorizontalAlignment="Stretch"
                ItemsSource="{x:Bind ViewModel.Categories, Mode=OneWay}"
                SelectedItem="{Binding Filter.Category, Mode=TwoWay}"
                SelectionChangedTrigger="Committed">
                <ComboBox.Header>
                    <NavigationViewItemHeader x:Uid="photo_Category" Content="Category" />
                </ComboBox.Header>
                <i:Interaction.Behaviors>
                    <ic:EventTriggerBehavior EventName="SelectionChanged">
                        <ic:InvokeCommandAction Command="{x:Bind ViewModel.SelectionChangedCommand}" />
                    </ic:EventTriggerBehavior>
                </i:Interaction.Behaviors>
            </ComboBox>
        </StackPanel>
    </Grid>
</UserControl>
