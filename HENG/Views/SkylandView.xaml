<Page
    x:Class="HENG.Views.SkylandView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:ic="using:Microsoft.Xaml.Interactions.Core"
    xmlns:local="using:HENG.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ucs="using:HENG.UserControls"
    mc:Ignorable="d">

    <i:Interaction.Behaviors>
        <ic:EventTriggerBehavior EventName="Loaded">
            <ic:InvokeCommandAction Command="{x:Bind ViewModel.LoadedCommand}" />
        </ic:EventTriggerBehavior>
    </i:Interaction.Behaviors>

    <Grid>
        <ucs:PhotoControl
            DownloadCommand="{x:Bind ViewModel.DownloadCommand}"
            ErrorVisibility="{x:Bind ViewModel.ErrorVisibility, Mode=OneWay}"
            LoadingVisibility="{x:Bind ViewModel.LoadingVisibility, Mode=OneWay}"
            Photos="{x:Bind ViewModel.Photos, Mode=OneWay}"
            RefreshCommand="{x:Bind ViewModel.RefreshCommand}" />
    </Grid>
</Page>
